# 更新记录

每个版本的最新一节会被 `npm run release` 直接作为 GitHub Release 的说明正文（见 `zotero-plugin.config.ts` 里的 `release.changelog`）。

## v1.7.0 — 2026-09-21

### 引用数

- **来源可以多选**：勾选多个来源时全部查询，取其中**最大**的数字显示；只勾选一个时仍然严格只看那一个来源，不会用别的来源顶替。
- 右键菜单新增 **刷新引用数**，与点赞数刷新彻底分开：只重新读取引用数，并会先解除 Google Scholar 的暂停状态立即重试。
- Google Scholar 的读取改为**全自动**：被拦住的头两次静默等待后自动重试（10 分钟起，逐次翻倍，最长 2 小时），连续多次仍未成功才提示一次，说明原因、还剩多久重试以及如何自己完成验证。
- 「打开 Google Scholar 验证页」现在会**带上当前论文标题直接搜索**，而不是打开一个空白搜索页；新增 **选择 Google Scholar 文献**，可以从搜索结果里挑一条，引用数按所选的记录显示，并记住这个选择（之后的自动读取也用它）。

### 点赞

- **刷新点赞数真的会刷新了**：刷新过程中单元格显示读取状态，失败时保留上一次的数字，并在结束时汇总「重新读取 / 失败 / 跳过」各多少条。以前只有「清除数据」看起来有效，是因为刷新没有任何反馈。

### arXiv 匹配

- 修复 **查找 arXiv…** 里「明明搜到了却选不中」的问题：候选行现在可以点击选择、双击直接应用；对话框一打开就自动搜索（不再默认显示「没有找到」）；候选门槛放宽，弱一些但正确的匹配也能选中；已经是 arXiv 的条目同样能在结果里找到自身。

### 外观

- **引用列可以有自己的样子**：引用数的样式、配色、阈值与范围筛选与点赞分开设置，用「与点赞数外观关联」开关切换——关联时只改点赞、引用自动跟随，不关联时各调各的。
- 每个颜色输入框旁边新增 **16 色色板**，点一下即可选色，不必再手输十六进制。
- 设置面板中引用来源改为三个复选框，来源说明文字同步重写。

## v1.6.0 — 2026-09-21

- 引用数**只显示你选中的那一个来源**（默认 Google Scholar），不再按「权威顺序」回退到别的来源。
- Google Scholar 被人机验证拦住时：暂停请求、弹一次提示、按 10 分钟起翻倍自动重试（最长 2 小时），并可右键打开验证页自己完成验证。

## v1.5.0 — 2026-09-21

- 外观样式精简为 **11 种**，名称不再带括号说明，「数字角标」完整显示数字。
- 范围筛选只保留「范围外变淡」；设置改动即时生效，不再需要手动刷新条目列表。

## v1.4.0 — 2026-09-20

- 引用数增加 **Google Scholar** 来源。
- 移除 CSV / JSON 导出、摘要笔记与一键「只看高赞」，插件只做列显示。
