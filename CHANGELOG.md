# 更新记录

每个版本的最新一节会被 `npm run release` 直接作为 GitHub Release 的说明正文（见 `zotero-plugin.config.ts` 里的 `release.changelog`）。

## v1.1.1 — 2026-09-21

首次以 **1.1.1** 发布的版本——版本号从这里重新开始，此前发布过的旧编号已全部删除。这一版就是目前的完整实现：

### 点赞数（alphaXiv）

- 从条目的 `URL`、`DOI` 或 `Extra` 识别 arXiv ID；**没有 ID 的条目自动匹配**：按 DOI 与标题查询多个免费学术 API，给候选打分，**只采用高置信度的结果**（中等相似度按“未找到”处理，不会猜、也不会弹窗让你确认）。
- 点赞数从 alphaXiv 页面读取，结果写进条目的 `Extra`（`alphaxiv_likes` 等），并记录**每日快照**，所以列里可以显示 `2979 ↑12` 这样的变化。

### 引用数

- 三个来源可选：**Google Scholar**（默认）、**OpenAlex**、**Semantic Scholar**。勾选一个来源时**严格只显示那一个**（口径不同，绝不拿别家数字顶替）；勾选多个时全部查询，显示其中**最大的数字**，并在鼠标提示里注明来源。
- Google Scholar 没有公开 API，插件读取公开搜索结果页上的 `Cited by`，并且**用浏览器的方式打开这一页**：在 Zotero 自己的隐藏浏览器里真实导航一次（会执行页面脚本、带上完整导航请求头），而不是只发一个普通请求——同一个地址、同一批 cookie，浏览器打得开而普通请求会被判成机器人。这条路径失败时自动退回普通请求，能用的那一种会被记住，之后的读取不再重复试探；两种都不行时报告会写清楚是「按地址限流」还是「要求人机验证」。**429/503 = 这个网络地址被限流**，**403 = 需要人机验证**：两种都会暂停并静默重试（10 分钟起逐次翻倍，最长 2 小时，连续多次仍未成功才提示一次），提示与列内说明分别讲清是哪一种，并给出验证页入口。
- 每个会话的**第一个** Google 请求是 `scholar.google.com` 首页本身（浏览器也是先到站点再搜索），Google 设下的 cookie 由 Zotero 自己的 cookie 罐接住并保留，之后的搜索都带着它们。非 Google 的 API（arXiv / OpenAlex / Semantic Scholar / Crossref）不受影响，仍然用 `AlphaLikes/<版本号>` 作为标识。

### 列与外观

- 点赞列与引用列都支持**固定阈值**或**按当前列表的分位数**三档着色；引用列可以有独立的样式、配色、阈值与范围筛选，也可以用「与点赞数外观关联」跟点赞保持一致。
- **11 种外观样式**；每个颜色框都是**取色面板**（明度/饱和度色域 + 色相条，一个圆点拖动取色，旁边有颜色预览），**自带配色的样式会跟着你选的颜色走**——形状与质感不变，只换色相；点「恢复这个样式的默认颜色」即可回到原生配色。
- 显示样式、高亮、范围筛选各自独立开关，「点赞数范围筛选」在引用设置之前；设置改动**即时生效**，不需要手动刷新。

### 读取互不等待

- **点赞与引用分开排队、各读各的**：以前所有请求共用一条队列，Google Scholar 的一次读取（打开页面 + 间隔 + 重试）会把点赞请求堵在后面，点赞列一直停在加载状态；现在**按网站分别排队**——同一个站点的间隔规则照旧（arXiv 3 秒、Scholar 15 秒），不同站点之间互不等待，点赞数通常几秒内就出现在列里。
- **读到一个就显示一个**：刷新一批条目时，每条读完立刻在自己的格子里显示数字，而不是等整批读完后一起出现——还在读的那些才显示加载状态。
- 勾选多个引用来源时，几个来源**同时查询**，不再一个等一个：Google Scholar 慢或没回应时，OpenAlex / Semantic Scholar 的结果不会被它拖住。
- **「刷新」是真的重新读取**：手动刷新不再复用一个刚由自动读取发起的请求，而是自己重新请求一次。

### 右键菜单与清除

- 五个动作在同一个分组里：**刷新 alphaXiv 点赞**、**刷新引用数**、**打开 Google Scholar 验证页**、**重置谷歌会话（清除 Google Cookie 后重试）**、**清除本插件写入的 Extra 记录**。
- 清除只删除本插件写在 `Extra` 里的行（`alphaxiv_*`），条目原有的其他内容**一行不动**；清除过的条目保持空白、不再被自动写回，手动刷新点赞或引用即可恢复（这份清单存在插件自己的设置里，不在 `Extra` 中，重启后依然有效）。

### 读不到的时候

- Google 请求的 User-Agent 与当前 Zotero 实际使用的引擎版本一致（Zotero 7 = Firefox 115、Zotero 9 = Firefox 140），并带上 `Sec-Fetch-*`、`Referer`、`Upgrade-Insecure-Requests` 这些浏览器导航会发的请求头。
- 设置面板「刷新与网络」里有 **诊断读取**：用当前设置**真的各请求一次** alphaXiv 与 Google Scholar（不读缓存、不改设置、不写 `Extra`），把请求地址、请求头（含 User-Agent）、HTTP 状态、异常、正文开头、代理设置、本次会话首个 Google 请求的结果，以及 **Google Scholar 两种读取方式各自的结果** 整理成一份报告，**复制到剪贴板**（同时写进 Zotero 的调试日志：帮助 → 调试输出日志，找 `[AlphaLikes]` 开头的行）。
- **读不到数字时格子留空**（不再显示「N/A」），鼠标停上去说明是哪一种失败：**403 被拒绝 / 429 限流 / 其他 4xx / 5xx / 网络·DNS·代理·超时 / 空响应 / 页面里确实没有那个数字**。
- Google 的请求不再带着 Zotero 自己的标识（Zotero 会给每个请求的 User-Agent 追加 `Zotero/<版本号>`，那本身就是「不是浏览器」的声明；Google Scholar 对带着它的请求回答 429，而同一台机器上的浏览器打开同一页却正常）；现在 Scholar 的请求与浏览器发出的**一模一样**：真实页面导航、不含应用名的 User-Agent、导航请求头、首页先行、以及 15 秒的最小间隔。
- 连续 **4 次**被拒后**停止自动重试**（继续重试只会让 Google 的判定更差——之前会一直数到第 31 次），并提示你两个办法：自己打开验证页完成一次，或用右键 →「**重置谷歌会话**」——后者会清掉 Zotero 这边的 Google Cookie 再重试，等同于换一个从没来过的浏览器。

### 兼容

- Zotero **7 – 10**（每次发布前在四个真实版本上各跑一遍完整测试套件）。
