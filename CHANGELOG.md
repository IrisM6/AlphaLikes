# AlphaPulse

**AlphaPulse**（原名 AlphaLikes）给 Zotero 加两列：**alphaXiv 点赞数** 和 **引用数**（默认走 Google Scholar）。装了就能用，不用填 API key、不用注册、也不用手动绑定论文。数字写在条目的 `Extra` 字段里，可以随文献库同步。

## 这一版的新东西

- **改叫 AlphaPulse 了。** 插件名、设置面板、提示文案和商店条目里的名字都换成 AlphaPulse；内部的 add-on ID（`alphalikes@iris`）和仓库地址保持不变，所以老用户照常自动升级，不需要重装。
- **多了一个入口：工具 → AlphaPulse（或右键条目 → AlphaPulse）。** 一个图标加插件名，悬停展开全部功能；原来散在右键里的五项收进这一个菜单，工具菜单里那份永远在。
- **新增「打开 alphaXiv 页面」**：在浏览器里打开这篇论文的 alphaXiv 页面，也就是点赞数真正的出处；条目还没识别出 arXiv ID 时会自动隐藏这一项。
- Zotero 11 的适配：同意 cookie 的写入按当前 Gecko 认识的时间单位走（见下）。

## 一、alphaXiv 点赞数

论文在 alphaXiv 上的点赞数——比引用数反应快得多，新论文有没有人在读，一眼就看出来。

- 从条目的 `URL`、`DOI` 或 `Extra` 识别 arXiv ID；没有 ID 的条目按 DOI 与标题查询学术 API，**只采用高置信度的结果**，中等相似度按「未找到」处理，不会猜。
- 滚到哪读到哪，**每读完一条就显示一条**，不用等整批读完。
- 记录**每日快照**，列里可以显示 `2979 ↑12` 这样的变化。

## 二、引用数

默认 **Google Scholar**，也可以换成 OpenAlex 或 Semantic Scholar：勾一个就只显示那一个，勾多个时显示其中最大的数字，悬停可以看到来源。

- 引用数变化慢，缓存时间比点赞数长，可以单独设置外观与阈值。
- Google Scholar 没有公开 API，读取的是搜索结果页上的 `Cited by`；被限流（429）或要求人机验证时会自动暂停并按 10 分钟、20 分钟、40 分钟……（最长 2 小时）重试，**不会用别的来源顶替**，同时其它来源照常自动填充。
- Google Scholar 只在选中条目或手动刷新时读取，避免把自己送进验证。

### 读取节奏

机器一样的节奏是要被拦的，所以读取按范围随机安排：

- 两次搜索之间**随机等 16–30 秒**；
- 打开页面后先向下滚一点，**停留约 3 秒**再读，页面里的脚本也趁这段时间跑完；
- 每读完 **2–5 条**，**歇 10–20 分钟**再继续（正在读的那几个格子会写着还剩几分钟）。

这四组范围都能在 设置 → AlphaPulse 里调整，每个输入框旁边都写着建议值，改完对下一次搜索就生效。

## 操作

右键任意条目：**刷新点赞数**、**刷新引用数**、**在浏览器中打开 Google Scholar 搜索页**（自己核对数字用）、**重置谷歌会话**、**清除本插件写入的 Extra 记录**。

两个刷新动作**各读各的**，互不影响，也都只作用在选中的条目上；读取期间单元格显示 `…`，完成后弹一条结果提示。清除范围严格限定在本插件自己写的那几行，条目里其它内容原样保留。

## 外观

- 两列都可按**固定阈值**或**当前列表的分位数**分档着色，也能按点赞 / 引用数区间筛选（范围外的条目整格变淡）。
- **11 种外观样式**（玻璃胶囊、莫兰迪低饱和、双色拼接……），每个颜色框都是取色面板（色域 + 色相条），也可以直接输入 CSS 颜色，随时可恢复默认。
- 显示样式、着色、范围筛选各自独立；引用数可以跟随点赞的外观，也可以单独设置。

## 读不出来时

失败的格子会告诉你是怎么回事，**以及多久之后自动重试**——不是只说一句「读取失败」：

- 悬停提示形如「读取失败：被限流（HTTP 429）· 约 15 分钟后自动重试」；
- 刷新结束后的提示里也会写「N 条未能读取（保留原值，约 X 分钟后自动重试）」，原来的数字不会被清掉；
- 碰巧正赶上「休息」的格子，会写着这一批已经读满、还剩几分钟继续。

## 兼容与隐私

- 支持 **Zotero 7 / 8 / 9 / 10 / 11**：范围只在一个地方定义，manifest、README 和包描述由测试与静态检查盯着不能让它们走散；插件只用 Zotero 7 起就有的接口，可选接口一律先探测再调用。（发布前在 7.0.32 / 8.0.4 / 9.0.6 / 10.0.3 和 11.0 开发版这几个真实版本上各跑一遍完整测试套件。）Zotero 11 用的是 Gecko 145，那里的 cookie 接口把过期时间当**毫秒**读：老写法传秒值不会被报错，只会被当成 1970 年，于是同意 cookie 静悄悄写不进去、每次读引用都落在 Google 的同意页上。现在按接口本身（`maybeCapExpiry`）选时间单位，两边都对。
- 所有记录都写在条目的 `Extra` 字段里（`alphaxiv_*` 行），可随文献库同步；联网请求只发往 alphaXiv、arXiv、OpenAlex、Semantic Scholar、Crossref、Google Scholar 等学术站点，不发送文献库内容或个人信息。
