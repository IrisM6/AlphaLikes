# AlphaPulse

**AlphaPulse** 给 Zotero 加两列：**alphaXiv 点赞数** 和 **引用数**（默认走 Google Scholar）。装了就能用，不用填 API key、不用注册、也不用手动绑定论文。数字写在条目的 `Extra` 字段里，可以随文献库同步。

## 最近修的东西

**v1.3.9 —— 两个数字分开读：引用数要慢，点赞数不用陪它一起等。**

- **新加的文献马上读点赞数。** 之前只要有一批引用数正在读，新加进来的条目点赞单元格就一直停在 `…`，其实它一个请求都没发出去——那批引用数按 16–30 秒的节奏一条条走，等下来就是好几分钟，看起来像是「点赞量读取被引用量拖住了」。现在正在读引用数的那批条目之外，新条目**当场读点赞数**，两者互不影响。（反过来也一如既往：读数期间不会顺手去读引用数。）

**v1.3.8 —— 修的是「读到一半卡住」这一类问题：有的页面明明带着引用数却被当成被限流，有的论文在 alphaXiv 上根本没有条目，有的等待其实只是排队。**

- **页面说了算，状态码不算。** 只要搜索结果页里有 `Cited by`（或 `gs_res_ccl_mid`/结果容器、中文界面的「被引用次数」）字样，这就是一次成功的读取——哪怕 HTTP 状态是 403/429：不会再被当成「人机验证」而把整个队列停下十分钟，也不会报「未能读取」。只有页面上**一点搜索痕迹都没有**（真正的 Sorry 页、同意页、验证码）才按被拦处理。
- **计数取的是「对得上的那一篇」的结果块。** 搜索页第一条不一定是要找的论文，而页面上的第一个 `Cited by` 属于第一条；现在按标题相似度挑出对应结果，再读**它自己**块里的数字。找不到对应结果 → 上一次那版的做法（直接说明「没有高置信度的对应文献」）。
- **等待中的条目不再被说成失败。** 之前会出现「已重新读取 0 条引用数；11 条未能读取（保留原值，约 10 分钟后自动重试）」——其实那 11 条只是在排队。现在被拒绝的那一条照实说失败并给出重试时间，排在它后面的条目写**「已加入等待列表（读取按顺序进行，轮到时自动读取，无需操作）」**。
- **alphaXiv 上没有的论文不再显示「读取失败」。** 点赞列留空，悬停写明**「alphaXiv 上没有找到这篇论文，没有点赞数可读（不会再自动重试）」**；这类条目不占用重试、也不算失败（汇总里单独列为「n 条不在 alphaXiv 上」）。没有 DOI、没有 arXiv ID、标题又太短、连检索都无从下手的条目也按这条处理——不再出现「标题太短，无法自动检索」这种说法。
- **新增「清空等待时间（取消读取尝试）」按钮**（设置 → AlphaPulse，读取进度那一段下面）：一键清掉等待列表和上面的倒计时，之后不会再自动重试；**已经读到的数字和写入 `Extra` 的记录原样保留**。按完会写明取消了几条。想再读就用右键 → **刷新引用数**。
- 下载的文件名形如 `alphapulse-v1.3.9.xpi`。

## 一、alphaXiv 点赞数

论文在 alphaXiv 上的点赞数——比引用数反应快得多，新论文有没有人在读，一眼就看出来。

- 从条目的 `URL`、`DOI` 或 `Extra` 识别 arXiv ID；没有 ID 的条目按 DOI 与标题查询学术 API，**只采用高置信度的结果**，中等相似度按「未找到」处理，不会猜。
- 滚到哪读到哪，**每读完一条就显示一条**，不用等整批读完。
- 记录**每日快照**，列里可以显示 `1130 ↑12` 这样的变化。

## 二、引用数

默认 **Google Scholar**，也可以换成 OpenAlex 或 Semantic Scholar：勾一个就只显示那一个，勾多个时显示其中最大的数字，悬停可以看到来源。

- 引用数变化慢，缓存时间比点赞数长，可以单独设置外观与阈值。
- Google Scholar 没有公开 API，读取的是搜索结果页上的 `Cited by`；被限流（429）或要求人机验证时会自动暂停并按 10 分钟、20 分钟、40 分钟……（最长 2 小时）重试，**不会用别的来源顶替**，同时其它来源照常自动填充。
- Google Scholar 只在选中条目或手动刷新时读取，避免把自己送进验证。
- 检索依据按 DOI、arXiv ID、网址、**标题**的顺序找：前三个都没有时用标题检索（标题至少 10 个字符）；搜索结果里没有「被引用次数」的论文按 **0** 记——0 是读数，不是失败。
- 搜索页里没有高置信度的对应文献时，格子显示 `N/A`，悬停写着**没有高置信度的对应文献（已按标题搜索）**：这是结果，不是失败，也不会再排队重试。
- 只要页面上有 `Cited by`（或结果容器、「被引用次数」）字样，就按**读取成功**处理，不管 HTTP 状态码是什么；计数取的是**标题对得上的那一条结果自己**的数字。真正一点搜索痕迹都没有的页面（Sorry 页、同意页、验证码）才按被拦处理、暂停并重试。
- 排在等待列表里的条目在汇总里写**「已加入等待列表」**，不是「未能读取」；被拒绝的那一条才写失败并给出重试时间。

### 读取节奏

机器一样的节奏是要被拦的，所以读取按范围随机安排：

- 两次搜索之间**随机等 16–30 秒**；
- 打开页面后先向下滚一点，**停留 4–8 秒**再读，页面里的脚本也趁这段时间跑完；
- 每读完 **8–15 条**，**歇 15–40 分钟**再继续（正在读的那几个格子会写着还剩几分钟）。

这四组范围都能在 设置 → AlphaPulse 里调整，每个输入框旁边都写着建议值；下面还有一行**「当前：…」**，把当前这八个数字连成一句话（停留写成 4–8 秒 这样的区间），改一个字段它就跟着变，改完对下一次搜索就生效。

想看当前进度，不用打开设置：菜单最上面就是它——右键选中几个条目，**每个条目各占一行**，写的是这一条自己的事：「〈论文标题〉：本条已请求 3 次 · 约 5 分钟后自动重试」。读到的数字一落地，这条的计数就清零；还没轮到的条目写 0，不会借用别的条目的数字，也不是整场会话的总数。设置面板里那行同样只列文献，写明是哪个条目、还要等多久。

**同一个等待只有一个时间**：Google 拒绝时弹的那条提示、格子悬停里的「约 N 分钟后自动重试」、菜单里那一行、设置里那一行，说的都是同一个时刻——不会再出现「弹窗说 5 分钟、旁边的字写着现在」。

**所有文献共用同一个等待列表。** 有文献在等重试时，新添加的条目排在后面（菜单里写着「排队等待读取（前面还有 N 条）」），不插队、也不借用老条目的倒计时；只有确认没有任何文献正在读取时才发起下一次搜索，两次搜索之间同样隔着上面的随机间隔。老条目的「马上自动重试」由列表自己的定时器兑现，不需要再按一次刷新。

## 操作

右键任意条目：**刷新点赞数**、**刷新引用数**、**在浏览器中打开 Google Scholar 搜索页**（自己核对数字用）、**手动填写引用量…**、**清除本插件写入的 Extra 记录**。

两个刷新动作**各读各的**，互不影响，也都只作用在选中的条目上；读取期间单元格显示 `…`，完成后弹一条结果提示。清除范围严格限定在本插件自己写的那几行，条目里其它内容原样保留。

## 外观

- 两列都可按**固定阈值**或**当前列表的分位数**分档着色，也能按点赞 / 引用数区间筛选（范围外的条目整格变淡）。
- **11 种外观样式**（玻璃胶囊、莫兰迪低饱和、双色拼接……），每个颜色框都是取色面板（色域 + 色相条），也可以直接输入 CSS 颜色，随时可恢复默认。
- 显示样式、着色、范围筛选各自独立；引用数可以跟随点赞的外观，也可以单独设置。

## 引用量也能自己填

Google Scholar 读不到（被拦、遇到验证码、这篇在它的库里叫别的名字）时，右键 → **手动填写引用量…**，填进去的数字就显示在引用列里，并且**自动读取不会再覆盖它**；把输入框清空就交回自动读取。悬停会写明这个数字是手动填的，不会算到任何来源头上。选中多条时这一项会隐藏——一个数字只属于一条。

## 读不出来时

失败的格子会写明原因，**以及多久之后自动重试**——不是只说一句「读取失败」：

- 悬停提示形如「读取失败：被限流（HTTP 429）· 约 15 分钟后自动重试」；
- 被 Google 拦住时，插件**自己**清掉这边的 Google Cookie、下一轮重试重新打开 Scholar 首页——这是后台动作，菜单里没有这一项，也不需要任何操作；
- 刷新结束后的提示里也会写「N 条未能读取（保留原值，约 X 分钟后自动重试）」，原来的数字不会被清掉；
- 碰巧正赶上「休息」的格子，会写着这一批已经读满、还剩几分钟继续；
- 格子显示 `N/A`、悬停写着**「没有高置信度的对应文献」**时，说明 Google Scholar 已经按标题搜过、没有找到对应的文献：这是结果，不是失败，不用等重试。想再试一次就用右键 → **刷新引用数**。

## 兼容与隐私

- 支持 **Zotero 7 / 8 / 9 / 10 / 11**：范围只在一个地方定义，manifest、README 和包描述由测试与静态检查盯着不能让它们走散；插件只用 Zotero 7 起就有的接口，可选接口一律先探测再调用。（发布前在 7.0.32 / 8.0.4 / 9.0.6 / 10.0.3 和 11.0 开发版这几个真实版本上各跑一遍完整测试套件。）Zotero 11 用的是 Gecko 145，那里的 cookie 接口把过期时间当**毫秒**读：老写法传秒值不会被报错，只会被当成 1970 年，于是同意 cookie 静悄悄写不进去、每次读引用都落在 Google 的同意页上。现在按接口本身（`maybeCapExpiry`）选时间单位，两边都对。
- 所有记录都写在条目的 `Extra` 字段里（`alphaxiv_*` 行），可随文献库同步；联网请求只发往 alphaXiv、arXiv、OpenAlex、Semantic Scholar、Crossref、Google Scholar 等学术站点，不发送文献库内容或个人信息。
